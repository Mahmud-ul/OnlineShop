@using EcommerceApp.ViewModel.Product;
@using EcommerceApp.ViewModel.Category;
@using EcommerceApp.ViewModel.Brand;
@model ProductUpdateViewModel
@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="box box-primary">
    <div class="box-header with-border">
        <h2 class="text-center">Update Product</h2><hr />
    </div>
    <!-- /.box-header -->
    <!-- form start -->
    <div class="row">
        <div class="col-md-8">
            <form method="post" asp-controller="Product" asp-action="Update" asp-route-id="@Model.Id" enctype="multipart/form-data" role="form">
                <div class="box-body">
                    <div class="form-group">
                        <label asp-for="@Model.Name">Name</label>
                        <input type="text" class="form-control col-md-5" asp-for="@Model.Name" required="required" placeholder="Enter Product Name"><br>
                        <span asp-validation-for="@Model.Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Price">Price</label>
                        <input type="text" class="form-control col-md-5" asp-for="@Model.Price" required="required" placeholder="Enter Product Price"><br>
                        <span asp-validation-for="@Model.Price" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Quantity">Quantity</label>
                        <input type="text" class="form-control col-md-5" asp-for="@Model.Quantity" required="required" placeholder="Enter Product Quantity"><br>
                        <span asp-validation-for="@Model.Quantity" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="CategoryId">Category Name</label>
                        <select class="form-control col-md-3" asp-for="CategoryId">
                            <option value="">Choose Category</option>
                            @foreach (CategoryCreateViewModel item in ViewBag.SubCategories)
                            {
                                if (@item.MainCat != null)
                                {
                                    <option value="@item.Id">@item.Name</option>
                                }
                            }
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="BrandId">Brand Name</label>
                        <select class="form-control col-md-3" asp-for="BrandId">
                            <option value="">Choose Brand</option>
                            @foreach (CreateBrandViewModel item in ViewBag.Brands)
                            {
                                <option value="@item.Id">@item.Name</option>
                            }
                        </select>
                        <span asp-validation-for="BrandId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Detail">Detail</label>
                        <textarea class="form-control col-md-5" asp-for="@Model.Detail" required="required"></textarea><br>
                        <span asp-validation-for="@Model.Detail" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Description">Specification</label>
                        <textarea class="form-control col-md-5" asp-for="@Model.Description" required="required"></textarea><br>
                        <span asp-validation-for="@Model.Description" class="text-danger"></span>
                    </div>
                    <div class="checkbox">
                        <label>
                            <input type="checkbox" asp-for="@Model.Featured"> Featured
                        </label>
                    </div>
                    <div class="checkbox">
                        <label>
                            <input type="checkbox" asp-for="@Model.Status"> Status
                        </label>
                    </div>
                    <!-- form End -->
                </div>
                <!-- /.box-body -->
                <div class="box-footer">
                    <button type="submit" class="btn btn-success">Update</button>
                    <a asp-controller="Product" asp-action="Index" class="btn btn-primary">Back to List</a>
                    <span class="text-danger">@ViewBag.ErrorMessage</span>
                </div>
            </form>
        </div>
    </div>
</div>

